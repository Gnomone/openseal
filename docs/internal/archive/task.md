# Task Checklist: Local Verification & N100 Preparation

- [x] **Phase 1: Cleanup & N100 Preparation**
    - [x] Identify legacy SQL/config files for archiving
    - [x] Propose and execute archiving of `sql/` and redundant `migrations/`
    - [x] Revert local simulation hacks (Delete `docker-compose.local.yml`, Restore `.env`)
    - [x] Update `scripts/dev-start.sh` to use consolidated `000_init.sql`
- [x] **Phase 2: Environment Alignment**
    - [x] Fix `validateEnv.ts` to allow `POSTGRES_*` fallback
    - [x] Modify `dev-start.sh` to prioritize `.env.local` for Docker
    - [x] Reset local DB data and verify successful connection via `npm run dev:local`
    - [x] Fix `dashboard/.env.local` to point to local backend (CORS fix)
- [x] **Phase 3: Synchronization**
    - [x] Push verified code to `HighStation_private`
    - [x] Hardened `.gitignore.private` & `.gitignore.public` (Exclude `data/`)
    - [x] Codified "Git Safety Protocol" in `지시문`
    - [ ] (Optional) Sync to public repo if requested
- [x] **Phase 4: N100 Deployment Preparation**
    - [x] Verify `migrations/000_init.sql` consistency
    - [x] Ensure `docker-compose.yml` is production-ready
- [x] **Phase 5: Service Discovery & Connection Refinement**
    - [x] Update backend `test-connection` logic with path probing
    - [x] Re-order `CreateService.tsx` wizard steps (Capabilities before Network)
    - [x] Update `DEMO_SERVICE_DEFAULTS` in `config.ts`
    - [x] Verify live integrity check with defined endpoints
- [x] **Phase 6: OpenSeal & Crypto Oracle Integration**
    - [x] Implement standard `/.openseal/identity` endpoint in `openseal-runtime`
    - [x] Fix `os error 2` during app spawn (Shell wrapper + Env preservation)
    - [x] Release **OpenSeal v0.2.4** (Daemon Mode + Symlink Safety)
    - [x] Fix OpenSeal runtime timeout (increased to 30s)
    - [x] Update `crypto-price-oracle` with official OpenSeal manifest
    - [x] Verify end-to-end registration flow in HighStation Dashboard
- [ ] **Phase 7: OpenSeal UX & Automation Roadmap**
    - [x] Draft "One-Step Integrity Execution" report
    - [x] Implement "Dependency Ghosting" (Auto-symlink) in v0.2.3
    - [x] Implement "Daemon Mode" in v0.2.4 for background execution
    - [x] Establish "Private Backup Protocol" with `.gitignore.private`
