# [보고서] OpenSeal UX 개선 제안: "One-Step Integrity" 가전

현재 OpenSeal을 사용하여 서비스를 배포할 때 발생하는 수동 작업(의존성 설치, 환경 확인 등)을 자동화하여, 개발자가 보안에만 집중할 수 있는 **제로 컨피그(Zero-config)** 환경을 지향합니다.

## 1. 현재의 페인 포인트 (Friction Points)
*   **의존성 단절**: `build` 시 소스 코드만 보호된 폴더로 복사되기 때문에, `node_modules`나 `venv` 같은 대규모 라이브러리가 유실되어 실행이 실패함.
*   **수동 링크 작업**: 개발자가 매번 `ln -s` 등으로 의존성을 수동으로 연결해줘야 함.
*   **환경 미비**: 서버에 `node`나 `npm` 자체가 없을 경우, OpenSeal이 단순히 "명령어 찾을 수 없음" 에러만 내뱉고 종료됨.

---

## 2. 기술적 개선 제안 (Technical Proposals)

### 🚀 A. Universal Dependency Ghosting (범용 의존성 자동 연결)
언어별 에코시스템을 자동 감지하여 실행 환경과 원본 의존성을 투명하게 연결합니다.
*   **작동 방식**: 
    1.  **Node.js**: `package.json` -> `node_modules` 심볼릭 링크.
    2.  **Python**: `requirements.txt`/`venv` -> 가상환경 경로 주입.
    3.  **Go/Rust**: 컴파일된 바이너리와 소스 간의 빌드 아티팩트 매핑.
*   **이점**: 언어에 상관없이 `openseal build && openseal run` 만으로 즉각적인 "증명 가능한 실행" 가능.

### 🛡️ B. Security-First Discovery (보안 우선 식별 로직)
사용자가 지적하신 **"과도한 무시(Over-ignoring)로 인한 검증 무력화"**를 방지하기 위한 엄격한 규칙을 적용합니다.
*   **작동 방식**:
    1.  **Strict Default**: 핵심 실행 파일(`.js`, `.py`, `.rs` 등)은 절대 자동으로 `.opensealignore`에 등록하지 않음.
    2.  **Audit Mode**: 빌드 시 "검증 대상에서 제외된 파일 리스트"를 요약 보고서로 출력하여, 사용자가 의도치 않게 핵심 로직을 제외했는지 육안 확인 유도.
    3.  **Integrity Threshold**: 전체 파일 중 일정 비율 이상이 Ignored/Mutable로 설정될 경우 보안 경고(Security Alert) 발생.
*   **이점**: 편의성을 챙기되, OpenSeal의 본질인 "코드 무결성 증명"의 신뢰도를 훼손하지 않음.

### 🏥 B. Runtime Pre-flight Check (런타임 사전 점검)
`openseal run` 실행 직후, 필요한 실행 환경이 갖춰졌는지 진단합니다.
*   **작동 방식**: 
    1.  `exec` 명령어가 `npm`으로 시작하면 `which npm` 실행.
    2.  없을 경우 단순히 에러를 내는 대신, `sudo apt install nodejs`와 같은 운영체제 맞춤형 가이드 출력.
*   **이점**: 인프라 숙련도가 낮은 사용자도 막힘 없이 설치 진행 가능.

### 🛡️ C. Smart Mutable Discovery (지능형 가변 파일 감지)
무결성 해시(A-Hash)에서 제외해야 할 파일들을 자동으로 식별합니다.
*   **작동 방식**: `.env`, `*.log`, `local.db` 등 흔히 쓰이는 가변 파일 패턴을 빌드 시 자동으로 `.openseal_mutable`에 추천/등록.
*   **이점**: 설정 누락으로 인해 해시값이 계속 변하는 오류를 사전에 방지.

---

## 3. 로드맵 (Roadmap)
1.  **v0.3.0**: Node.js 우선 지원 (`node_modules` 자동 링크)
2.  **v0.4.0**: 환경 진단(Doctor) 기능 추가
3.  **v0.5.0**: Python(`venv`) 및 기타 언어 확장

---

**결론**: OpenSeal은 "보안 도구"를 넘어, 개발자가 별도의 인프라 지식 없이도 **"증명 가능한 서비스"**를 즉시 띄울 수 있는 **통합 런타임 플랫폼**으로 진화해야 합니다.
